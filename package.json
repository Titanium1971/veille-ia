{
  "name": "veille-ia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily AI-news digest, scheduled via GitHub Actions, summarized by Claude Agent SDK and emailed via Resend",
  "scripts": {
    "veille": "node scripts/index.mjs",
    "veille:dry": "node scripts/index.mjs --dry-run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.142",
    "resend": "^6.12.3",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
